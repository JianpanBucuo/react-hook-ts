import React from 'react'
import './index.scss'

interface DepartDateProps {}
const DepartDate: React.FC<DepartDateProps> = (props: DepartDateProps) => {
  return <div></div>
}
export default DepartDate
